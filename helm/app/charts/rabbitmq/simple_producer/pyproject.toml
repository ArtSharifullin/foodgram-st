[project]
name = "simple-producer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pika>=1.3.2",
]

[tool.uv.workspace]
members = [
    "simple_consumer",
]
