[project]
name = "load-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "locust>=2.43.3",
]

[tool.locust]
host = "http://foodgram.local/"
users = 10000
spawn-rate = 10
run-time = "10m"
